# ==========内核源，直接指向原版作者仓库，不要修改内核源码！！==========
KERNEL_SOURCE=https://github.com/Akitlove/android_kernel_xiaomi_sm7250
KERNEL_SOURCE_BRANCH=16-susfs-bak2
KERNEL_CONFIG=vendor/lito-perf_defconfig
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
KERNEL_NAME=picasso

# 编译工具链，沿用这套内核原本的编译配置
CLANG_BRANCH=master-kernel-build-2022
CLANG_VERSION=r450784e
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_CLANG=false
USE_LLVM=false

# =========最重要：none，脚本不做任何KSU打补丁、不下载SU源码==========
KSU_VARIANT=none
# 下面这一堆KSU相关参数，当variant=none的时候脚本会直接忽略，随便填也没关系
KSU_REF=
KSU_HOOK_MODE=manual
ENABLE_SUSFS=false
ENABLE_PATH_UMOUNT=false
ENABLE_HIDE_STUFF=false
ENABLE_KPM=false

# =========不要让脚本自动注入kprobes/overlayfs！！原作者defconfig已经写好了==========
ADD_KPROBES_CONFIG=false
ADD_OVERLAYFS_CONFIG=false

DISABLE_LTO=false
DISABLE_CC_WERROR=true

# EXTRA_DEFCONFIG：**空！！不要加任何CONFIG_KSU相关参数**，全部交给源码defconfig
EXTRA_DEFCONFIG=""

# 打包相关
NEED_DTBO=false
BUILD_BOOT_IMG=false
USE_CUSTOM_ANYKERNEL3=false

ENABLE_CCACHE=true
REMOVE_UNUSED_PACKAGES=true
